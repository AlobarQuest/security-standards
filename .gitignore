# Never commit secrets or local credentials (this tool, of all repos, must be clean)
.env
*.env
**/env
*-migration/
*.password
*_password
*.key
*.pem
id_*
.DS_Store

.venv/
.venv-events/
__pycache__/
*.egg-info/
.pytest_cache/

# provider-agent session state (stateful claude -p threads)
.provider-agent/sessions/

# factory-events local DB compose env (secrets)
deploy/factory-events-db/.env
